import 'package:flutter/material.dart';

class GdpScreen extends StatelessWidget {
  const GdpScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("GdpScreen");
  }
}
